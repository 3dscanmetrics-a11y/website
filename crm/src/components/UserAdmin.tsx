import { createUser } from '@/app/user-actions';
type UserRow={id:string;display_name:string;email:string;role:string;must_change_password:number};
export default function UserAdmin({users}:{users:UserRow[]}){
 return <section className="mt-6 bg-white border rounded-xl p-5"><h2 className="font-bold text-lg">CRM users and access</h2><form action={createUser} className="grid md:grid-cols-4 gap-3 my-4"><input required name="name" placeholder="Display name" className="border rounded p-2"/><input required type="email" name="email" placeholder="Email" className="border rounded p-2"/><select name="role" className="border rounded p-2"><option>CRM</option><option>FINANCE</option><option>HR</option><option>ADMIN</option></select><input required minLength={12} type="password" name="password" placeholder="Temporary password" className="border rounded p-2"/><button className="md:col-span-4 bg-gray-900 text-white rounded p-2">Create user</button></form><div className="divide-y">{users.map(u=><div key={u.id} className="py-2 flex justify-between text-sm"><span>{u.display_name} · {u.email}</span><span>{u.role}{u.must_change_password?' · password change required':''}</span></div>)}</div></section>;
}
