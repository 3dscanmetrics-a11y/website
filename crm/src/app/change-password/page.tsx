'use client';import {useActionState}from'react';import{changePassword}from'../auth-actions';export default function Page(){const[state,action,pending]=useActionState(changePassword,null);return <main className="min-h-screen bg-gray-950 flex items-center justify-center p-4"><form action={action} className="w-full max-w-sm bg-white rounded-2xl p-7 space-y-4"><h1 className="text-xl font-bold">Set a secure password</h1><p className="text-sm text-gray-500">Your bootstrap password must be replaced before using the CRM.</p><input required minLength={12} type="password" name="password" placeholder="New password" className="w-full border rounded-lg p-3"/><input required minLength={12} type="password" name="confirm" placeholder="Confirm password" className="w-full border rounded-lg p-3"/>{state?.error&&<p className="text-red-600 text-sm">{state.error}</p>}<button disabled={pending} className="w-full bg-cyan-500 rounded-lg p-3 font-semibold">Save password</button></form></main>}
